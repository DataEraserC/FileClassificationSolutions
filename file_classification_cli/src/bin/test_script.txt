# 这是一个测试脚本文件
# 创建一个新组
group create --name "测试组"

# 创建一个新标签
tag create --name "测试标签"

# 创建一个新文件
file create --type "text" --path "/path/to/test.txt" --group_id 1

# 列出所有组
group list-by-conditions -c "id>0"

# 列出所有标签
tag list-by-conditions -c "id>0"

# 列出所有文件
file list-by-conditions -c "id>0"

# 测试简化命令
ls